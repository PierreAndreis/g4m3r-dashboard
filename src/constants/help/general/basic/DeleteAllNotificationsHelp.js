import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Delete All Notifications?</Box.Title>
    <Box.Body padding>
      <p>
        The delete all notifications setting will enable/disable the deletion of all bot
        responses for alerts or notifications when running a command. For example, if you
        typed something wrong the bot will say this is incorrect please try again and to
        prevent clutter and spam of such messages you can enable deletion of these
        messages.
      </p>
    </Box.Body>
  </div>
);
