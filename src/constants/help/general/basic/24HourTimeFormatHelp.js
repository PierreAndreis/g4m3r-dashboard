import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a 24 Hour Time Format?</Box.Title>
    <Box.Body padding>
      <p>
        The 24 hour time format allows you to enable Military Time format for features
        that require time such as the events feature.
      </p>
    </Box.Body>
  </div>
);
