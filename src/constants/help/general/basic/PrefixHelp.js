import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Prefix?</Box.Title>
    <Box.Body padding>
      <p>
        The prefix is what tells the bot to listen to that message. If the prefix is set
        to "." the bot will only respond when starting your message with . as the first
        letter.
      </p>
    </Box.Body>
  </div>
);
