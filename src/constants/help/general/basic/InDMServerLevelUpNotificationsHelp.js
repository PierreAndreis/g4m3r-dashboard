import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a In DM Server Level Up Notification?</Box.Title>
    <Box.Body padding>
      <p>
        The in DM server level up notification messages are sent when a user types a
        message in a channel and they leveled up in the server's G4M3R leaderboards. The
        level up message can be sent to their DM.
      </p>
    </Box.Body>
  </div>
);
