import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Stories Trigger Deletion?</Box.Title>
    <Box.Body padding>
      <p>
        The stories trigger deletion enables or disables deleting the messages that
        trigger a story.
      </p>
    </Box.Body>
  </div>
);
