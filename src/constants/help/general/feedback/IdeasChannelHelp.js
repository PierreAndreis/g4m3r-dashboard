import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Ideas Channel?</Box.Title>
    <Box.Body padding>
      <p>
        The idea channel is where all the idea/suggestions feedback will be sent when a
        user sends feedback with the feedback command.
      </p>
    </Box.Body>
  </div>
);
