import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Bug Status?</Box.Title>
    <Box.Body padding>
      <p>
        The bug status will enable/disable allowing users to send feedback on issues in
        the server to a channel with a clean embed and 2 reactions for other users on the
        server to be able to vote on.
      </p>
    </Box.Body>
  </div>
);
