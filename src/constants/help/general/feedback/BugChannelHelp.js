import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Bug Channel?</Box.Title>
    <Box.Body padding>
      <p>
        The bug channel is where all the bug/issue/problem feedback will be sent when a
        user sends feedback with the feedback command.
      </p>
    </Box.Body>
  </div>
);
