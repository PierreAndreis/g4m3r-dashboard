import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Bug Color?</Box.Title>
    <Box.Body padding>
      <p>
        The bug color is the color of the embed that will be used when sending a bug
        feedback in the bug channel.
      </p>
    </Box.Body>
  </div>
);
