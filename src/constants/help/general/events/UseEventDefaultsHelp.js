import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is a Use Event Defaults?</Box.Title>
    <Box.Body padding>
      <p>
        The use event defaults setting enables using event defaults that you have created
        to serve as the event template when making events on your server.
      </p>
    </Box.Body>
  </div>
);
