import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Verification Category?</Box.Title>
    <Box.Body padding>
      <p>
        The verification category is where the bot will create new channels for the
        verification system and also the only category in which the verify command will
        work.
      </p>
    </Box.Body>
  </div>
);
