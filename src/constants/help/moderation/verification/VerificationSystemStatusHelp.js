import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Verification System Status?</Box.Title>
    <Box.Body padding>
      <p>
        This will enable or disable the verification system on your server. When enabling
        it the bot will create a new category, Verification Zone, and a new channel,
        #verify, where the verification process will take place. It will also create and
        set up the entire server with the verify role.{" "}
      </p>
    </Box.Body>
  </div>
);
