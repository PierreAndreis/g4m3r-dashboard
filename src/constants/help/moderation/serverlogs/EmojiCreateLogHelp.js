import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Emoji Create Logs?</Box.Title>
    <Box.Body padding>
      <p>
        Emoji create logs are posted whenever someone adds a new custom emoji to the
        server.
      </p>
    </Box.Body>
  </div>
);
