import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Member Add Logs?</Box.Title>
    <Box.Body padding>
      <p>Member add logs are posted whenever someone joins the server.</p>
    </Box.Body>
  </div>
);
