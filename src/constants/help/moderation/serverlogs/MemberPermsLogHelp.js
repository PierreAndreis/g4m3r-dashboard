import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Member Perms Logs?</Box.Title>
    <Box.Body padding>
      <p>
        Member permissions logs are posted whenever someone updates a members permissions
        on a server.
      </p>
    </Box.Body>
  </div>
);
