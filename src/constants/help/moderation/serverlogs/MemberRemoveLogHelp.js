import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Member Remove Logs?</Box.Title>
    <Box.Body padding>
      <p>
        Member remove logs are posted whenever someone leaves or is removed the server.
      </p>
    </Box.Body>
  </div>
);
