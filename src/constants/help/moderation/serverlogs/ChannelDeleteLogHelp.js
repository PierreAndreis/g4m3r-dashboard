import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Channel Delete Logs?</Box.Title>
    <Box.Body padding>
      <p>
        Channel delete logs are posted whenever someone deletes a channel on the server.
      </p>
    </Box.Body>
  </div>
);
