import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Server Mute Logs?</Box.Title>
    <Box.Body padding>
      <p>
        Server mute logs are posted whenever someone is muted and can no longer speak in
        voice channels on a server.
      </p>
    </Box.Body>
  </div>
);
