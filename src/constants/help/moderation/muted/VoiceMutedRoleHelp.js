import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Voice Muted Role?</Box.Title>
    <Box.Body padding>
      <p>
        This is the role that is assigned to users to prevent them from having permissions
        to voice in the server when they are muted.
      </p>
    </Box.Body>
  </div>
);
