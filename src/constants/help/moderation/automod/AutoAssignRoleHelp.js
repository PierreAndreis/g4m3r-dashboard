import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Auto Assign Role?</Box.Title>
    <Box.Body padding>
      <p>
        The auto assign role is automatically assigned to all users joining your server.
        If the verification system is enabled in your server, the auto assign role will
        only be assigned once the member has completed the verification process.
      </p>
    </Box.Body>
  </div>
);
