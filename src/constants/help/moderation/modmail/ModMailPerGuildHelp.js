import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Max Mails Per Guild?</Box.Title>
    <Box.Body padding>
      <p>
        Max mails per guild will set the maximum amount of mails allowed to be received at
        any given time on your server. Once reached, no new mails can be sent until one of
        them are closed.
      </p>
    </Box.Body>
  </div>
);
