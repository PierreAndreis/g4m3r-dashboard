import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Max Mails Per User?</Box.Title>
    <Box.Body padding>
      <p>
        Max mails per user will set the maximum amount of mails a user is allowed to be
        send at any given time on your server. Once reached, no new mails can be sent by
        that user until one of them are closed.
      </p>
    </Box.Body>
  </div>
);
