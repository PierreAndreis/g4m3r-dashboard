import React from "react";
import Box from "../../../../components/Box";

export default (
  <div>
    <Box.Title>What is Max Inactive Days Allowed?</Box.Title>
    <Box.Body padding>
      <p>
        The max inactive days allowed is the amount of days the bot checks to see if your
        guild mates have played a match. For example, if you want to be alerted when
        players have not played a match in over 3 days the max inactive day can be set to
        3.
      </p>
    </Box.Body>
  </div>
);
